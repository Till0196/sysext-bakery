# List of sysexts and versions to build by the release Github Action.
#
# This fork only ships fusermount3 — upstream extensions are intentionally
# omitted so the fork's CI does not attempt to mirror the entire upstream
# release catalog.

fusermount3 3.18.2
fusermount3 latest
